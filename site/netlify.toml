[build]
  command = "pip3 install -q poetry && poetry config virtualenvs.in-project true && poetry install -v && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && mkdocs build"
  publish = "site"

[context.production.environment]
  MKDOCS_ENV = "production"

